# 🎬 動画制作 管理表

動画制作スケジュールのスプレッドシートを読み込み、**1枚の見やすい表**として
表示するタブです（タスク管理アプリ内）。元のシートは変更しません（読み取り専用）。

## 何が見えるか

- **管理表（1テーブル）**：状態・アラート・締切・クライアント・企画・ステージ・
  担当・No.・投稿日・元素材/共有リンク を一覧表示
- **既定の並び順**：進行中（`済`/`ボツ`以外）を上に、その中で
  「①期限切れ → ②締切が近い順 → ③ステージ序盤順」
- **アラート列**：`⚠️ N日超過` / `🔥 本日締切` / `あとN日` を自動表示
- **絞り込み**：クライアント / 担当 / 表示（進行中のみ・すべて）/ 企画キーワード検索
- **列見出しクリックで並べ替え**

## 使い方

1. アプリを起動：`streamlit run app.py`
2. 上部タブの **🎬 動画制作** を開く

シートはデフォルトで既存の制作スケジュールを参照します。別シートを使う場合は
タブ内の「⚙️ 接続するスプレッドシート」にIDを入力してください。

### 初回のみ：再認証が必要

スプレッドシート読み取り権限（`spreadsheets.readonly`）を追加したため、
**初回は一度だけ再認証**が必要です。

```bash
rm token.json          # 既存トークンを削除
streamlit run app.py   # 起動時にブラウザで再認証
```

Streamlit Cloud で運用している場合は、新しいスコープを含む `token.json` の
内容を `st.secrets["google_token"]` に再設定してください。

## 設定（任意）

| 設定 | 方法 | 既定 |
| --- | --- | --- |
| 参照スプレッドシートID | 環境変数 `VIDEO_SHEET_ID` または `st.secrets["video_sheet_id"]`、もしくはタブ内の入力欄 | 既存の制作スケジュール |

## 仕組み（パーサ）

1枚のシートに制作リスト・断片メモ・カレンダー・分析が混在していても、
**「クライアント列」と「企画列」の両方が埋まっている行**だけを制作レコードと
みなして抽出します（`secretary/sheets_ops.py`）。表記ゆれ（`アフ`→`アフレコ`、
`M提出`/`C提出`→`提出`）やヘッダの注記（`サムネ(PSD含む)`）も正規化します。

パーサのテスト：

```bash
python tests/test_video_parser.py
```
